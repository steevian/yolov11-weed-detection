
# exp2 分析报告（任务1 + 任务2）

日期：2026-04-02

## 一、证据范围与说明

本报告基于以下已核验文件：

- `docs/exp1_log.md`
- `docs/exp2_plan.md`
- `docs/newmemory.md`
- `docs/exp2_log.md`
- `exp1/eval/baseline_test_results.json`
- `exp1/eval/mbv3_test_results.json`
- `exp1/eval/eca_test_results.json`
- `exp2/eval/p2_simam_test_results.json`
- `exp1/configs/yolov11s_mbv3.yaml`
- `exp1/configs/yolov11s_mbv3_eca.yaml`
- `exp2/configs/yolov11s_mbv3_p2_simam.yaml`
- `exp2/models/simam.py`
- `exp2/runs/p2_simam/results.csv`

补充说明：

1. `exp2/eval` 目录目前只有 `p2_simam_test_results.json`，其余三组（dwconv/shuffle/p075ghost）尚无评估 JSON。
2. 训练轮次核对结果：
	 - baseline/mbv3/eca：`results.csv` 均为 201 行（表头+200 epoch）
	 - p2_simam：`results.csv` 为 178 行（表头+177 epoch）
	 - 即 p2_simam 当前并非完整 200 轮结果。

---

## 二、任务1：两次改进失败原因的详细分析

## 2.1 现象总览（量化）

四组核心指标如下：

| Model | Precision | Recall | mAP50 | mAP50-95 | Params(M) | FPS |
|---|---:|---:|---:|---:|---:|---:|
| baseline | 0.9190 | 0.9208 | 0.9587 | 0.8927 | 9.4167 | 66.16 |
| mbv3 | 0.9332 | 0.9101 | 0.9598 | 0.8916 | 7.4648 | 45.40 |
| eca | 0.9329 | 0.9148 | 0.9571 | 0.8830 | 7.4648 | 47.23 |
| p2_simam | 0.9526 | 0.8890 | 0.9537 | 0.8760 | 7.6074 | 36.83 |

差值（相对比较）关键点：

- `mbv3 vs baseline`：
	- Precision `+0.0142`
	- Recall `-0.0107`
	- mAP50 `+0.0011`（近似持平）
	- mAP50-95 `-0.0011`
	- Params `-1.9518M`
	- FPS `-20.7588`
- `eca vs mbv3`：
	- Precision `-0.0003`（几乎不变）
	- Recall `+0.0046`（小幅回升）
	- mAP50 `-0.0027`
	- mAP50-95 `-0.0086`（下降明显）
	- Params 几乎不变
	- FPS `+1.8296`
- `p2_simam vs mbv3`：
	- Precision `+0.0194`
	- Recall `-0.0211`（降幅更大）
	- mAP50 `-0.0061`
	- mAP50-95 `-0.0156`
	- Params `+0.1426M`
	- FPS `-8.5702`

结论先行：

- 两次改进都出现同一个核心问题：`Precision 上升`但`Recall 与 mAP50-95 下滑`，即“更保守、更少误检”的同时“漏检增加”，综合检测质量未提升。

## 2.2 为什么 MBV3+ECA 失败（重点剖析）

### 2.2.1 结构层面：ECA 插入位置与任务不匹配

从 `yolov11s_mbv3_eca.yaml` 看，ECA 被放在 Backbone 投影后的三个尺度（P3/P4/P5）上，即全尺度通道重标定。

这类策略的问题在于：

1. 通道注意力会强化“高响应通道”，对弱纹理/遮挡/小目标的低响应特征不友好。
2. 在轻量主干（MBV3）下，表征冗余本就更少，再做全尺度门控，容易进一步损失细粒度信息。
3. 当前改动集中在 Backbone，Neck 多尺度融合能力并未增强，无法对 Recall 缺口形成有效补偿。

这与实测一致：Recall 虽比 mbv3 小幅回升，但 mAP50-95 明显下降，说明定位质量与中高 IoU 区间的综合性能在恶化。

### 2.2.2 指标层面：补偿方向偏离主目标

你的目标是轻量化后做精度补偿，关键应优先修复 Recall/mAP50-95。ECA 实测却是：

- Precision 基本不增
- mAP50、mAP50-95 下滑

说明“通道校准”不是当前瓶颈，瓶颈更可能在多尺度融合与小目标感受野信息的保留。

### 2.2.3 工程层面：轻量化目标未闭环

ECA 没有带来参数优势（与 mbv3 基本同量级），也没有换来精度收益，属于“额外结构复杂度但无净收益”的改造。

结论：MBV3+ECA 失败的本质是“改动点偏 Backbone 通道重标定，未命中 Recall/mAP50-95 的主要矛盾（融合与小目标特征保持）”。

## 2.3 为什么 MBV3+P2_SimAM 失败（重点剖析）

### 2.3.1 结构层面：P2 分支 + SimAM 插入后，模型偏向高置信保守预测

从 `yolov11s_mbv3_p2_simam.yaml` 看，模型新增了 P2 分支，并在高分辨率路径插入单点 SimAM。

`simam.py` 的实现是对空间维度做均值/方差式响应重标定（无参数但有显著逐元素计算）。

这会产生两个效应：

1. 对高响应区域更“自信”，Precision 提升。
2. 对弱响应目标抑制更强，Recall 下滑。

实测正是该模式：Precision 最高（0.9526），但 Recall 最低（0.8890），导致 mAP50 与 mAP50-95 同时下滑。

### 2.3.2 训练公平性层面：当前 p2_simam 未跑满 200 轮

这是关键风险点：

- baseline/mbv3/eca 均为 200 epoch。
- p2_simam 当前仅 177 epoch。

因此当前 p2_simam 结果虽能判断“方向上不理想”，但还不能作为最终定论版本写死到论文结论（建议标注“阶段性结果”）。

### 2.3.3 推理效率层面：参数小增但速度显著下降

`val_speed_ms.inference` 对比：

- baseline：32.05ms
- mbv3：45.86ms
- eca：37.78ms
- p2_simam：37.85ms

并且 benchmark FPS：

- mbv3：45.40
- p2_simam：36.83

说明 p2_simam 在当前实现下引入了额外运行时开销，未形成“精度收益 > 速度损失”的交换。

### 2.3.4 训练动态层面：曲线呈现“高 Precision、Recall 振荡且偏低”特征

从 `exp2/runs/p2_simam/results.csv` 可见，后段 epochs 中 Precision 经常维持较高，而 Recall 并未同步抬升到 baseline 水平，且有振荡。这与最终评估的“P 高、R 低”一致，不是单次评估偶然。

结论：MBV3+P2_SimAM 失败的本质是“注意力带来的保守判定倾向 + 插入位置与高分辨率路径耦合 + 训练未满轮导致结果稳定性不足 + 速度代价偏高”。

## 2.4 两次失败的共性根因（论文可用归纳）

1. 精度补偿方向偏“抑制误检”，而不是“减少漏检”。
2. 缺少针对 Recall/mAP50-95 的专项设计（仅加注意力不足以补偿轻量主干信息损失）。
3. 结构改动后未配套重新做针对性超参搜索（如损失权重、样本分配、增强强度）。
4. 评估闭环未完全完成（exp2 其余变体缺 JSON，p2_simam 未满轮），导致决策信息不完整。

---

## 三、任务2：你的“两阶段思路”是否可行？

结论：`可行`，而且是当前最合理路线之一。

你的思路本质是“先增后减”：

1. 先接受一定参数上涨，优先补回 Recall/mAP50-95；
2. 再在 Neck/Head 做二次轻量化，尽量把参数和速度拉回去；
3. 同时约束轻量化导致的精度回退。

这个思路符合你当前实验事实：现阶段最短板不是参数量，而是综合精度（尤其 Recall/mAP50-95）。

## 3.1 我建议的优化版方案（在你思路上改进）

我建议用“`三闸门两阶段`”而不是直接两步硬推：

### 阶段A（精度补偿阶段，允许增参）

目标：先把 `mAP50-95` 和 `Recall` 稳定拉回。

候选结构（优先级）：

1. `A1: MBV3 + P2（不加SimAM）`
2. `A2: MBV3 + P2 + BiFPN-Lite(1层可学习融合)`
3. `A3: 在 A2 基础上，仅单点注意力（P4 或 P5 低分辨率侧）`

说明：

- 先去掉高分辨率路径上的 SimAM 作为主增益来源，避免再出现“P高R低”放大。
- 先验证“P2+融合”本身是否能带来 Recall 恢复，再考虑注意力。

阶段A闸门（必须全部满足，才进入阶段B）：

- `Recall >= 0.915`（至少回到 eca 水平以上，逼近 baseline）
- `mAP50-95 >= 0.892`（追平 baseline）
- `mAP50 >= 0.958`
- 参数增幅相对 mbv3 可放宽到 `<= +15%`

### 阶段B（二次轻量化阶段，抵消增参）

目标：在尽量不伤精度前提下回收参数和速度。

建议只动 Neck/Head，按低风险顺序：

1. `B1: Neck普通Conv -> DWConv（优先 3x3 stride=2 路径）`
2. `B2: 部分 C3k2 通道系数缩放（先 0.875，再 0.75）`
3. `B3: 局部 Shuffle/Ghost 仅用于最耗时分支（逐点替换）`

阶段B闸门（相对阶段A最优模型）：

- `mAP50-95` 回退不超过 `0.003`
- `Recall` 回退不超过 `0.005`
- 参数至少回收 `8%`
- FPS 至少提升 `10%`

### 阶段C（最终定稿门）

从阶段A最优与阶段B最优中择优，形成论文主模型：

- 若部署优先：选阶段B最优
- 若精度优先：选阶段A最优
- 并保留另一模型作“工程-精度权衡对照”

## 3.2 最小可执行实验矩阵（节省训练成本）

考虑你当前训练成本较高，建议如下：

第一轮（快速筛选，50 epoch）：

- A1、A2、A3 各跑 50 epoch
- 用同一 seed=42 与同一评估参数
- 只保留前2名进入全量

第二轮（全量验证，200 epoch）：

- Top2 跑满 200 epoch
- 对最佳 A* 做 B1、B2 两个轻量化版本
- 再跑 200 epoch 得到 B1*、B2*

第三轮（论文确认）：

- A* 与 B* 各补 1 次复现实验（seed=43）
- 若两次结果波动：
	- `|ΔmAP50-95| <= 0.002` 判为稳定
	- `|ΔRecall| <= 0.003` 判为稳定

## 3.3 量化评价标准（建议写入论文方法章）

定义综合评分（用于内部筛选，不直接替代主指标）：

$$
S = 0.35\cdot mAP_{50-95} + 0.30\cdot Recall + 0.20\cdot Precision + 0.10\cdot FPS_{norm} + 0.05\cdot Params_{norm}^{-1}
$$

其中：

- $FPS_{norm}=FPS/FPS_{baseline}$
- $Params_{norm}=Params/Params_{baseline}$

使用方式：

1. 主决策仍以 `mAP50-95`、`Recall` 作为硬门槛；
2. 在满足硬门槛的候选中，用 $S$ 做排序。

---

## 四、对你当前路线的最终建议

1. 思路方向是对的：先补精度再回收参数，比“持续压参”更符合当前证据。
2. 立即补齐公平性：先把 `p2_simam` 补满 200 epoch（或明确早停条件），否则不建议作为最终负结论。
3. 阶段A不要再从“高分辨率 SimAM 强化”起步，优先走 `P2 + 融合增强`。
4. 阶段B严格做“小步可回滚”轻量化，不做大规模一次性替换。
5. 当前可作为阶段性论文表述：
	 - MBV3+ECA 与 MBV3+P2_SimAM 均未实现有效综合补偿；
	 - 下一轮将采用“先增后减”的可量化闭环策略，重点保障 Recall 与 mAP50-95。

## 五、待补数据清单（建议尽快完成）

1. 生成并归档：
	 - `exp2/eval/p2_simam_dwconv_test_results.json`
	 - `exp2/eval/p2_simam_shuffle_test_results.json`
	 - `exp2/eval/p2_simam_dwconv_p075ghost_test_results.json`
2. 为所有 exp2 模型补充：每类 AP/AR（per-class）导出。
3. 对阶段A/阶段B入围模型补充 2 个种子复现实验（42/43）。

以上三项完成后，论文中的“失败原因”和“改进方案有效性”会更有说服力。


